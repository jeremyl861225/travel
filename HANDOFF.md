# 進度交接 — 2026-08-29

## 現在的狀態：可以跑，但視覺還在調

App 本身功能完整、後端已上線、同步實測過。剩下的是視覺方向的最後幾個決定，
使用者正在挑，挑完套進去就收工。

---

## 已經完成

### 後端（已上線，不用再動）
- Supabase 專案 **`dawcpdgonxmhojwonkut`**（就是 todo-app 那個，因為免費方案每人只能
  有 2 個 active 專案，第三個建不起來）。資料表全部在獨立的 **`travel` schema**，
  跟 todo-app 的 public 表互不相干。
- Migration 已套用兩次：`travel_schema_init`、`travel_fix_ambiguous_columns`。
  第二次修的是 42702：`RETURNS TABLE` 的欄位名跟資料表欄位同名，
  `SET rev = rev + 1` 兩邊都說得通 → 每次推送都失敗。改成 `rev = cards.rev + 1`。
- Edge Function **`gmap`** 已部署（`verify_jwt: false`，驗證改由旅程代碼＋密碼負責）。
- **`GOOGLE_MAPS_API_KEY` 這個 secret 還沒設**，所以自動抓景點與自動算路程目前是停的，
  會回 `NO_API_KEY`，前端已經有降級處理（改手動填，不會整頁失效）。
  申請步驟寫在 `docs/GOOGLE-MAPS-SETUP.md`，要使用者自己去綁信用卡。

### 前端
- 單檔 `index.html`，2023 行，7 個 script 區塊，`node check.mjs index.html` 通過。
- 已驗證可用：建立旅程、解鎖、加卡、時刻推算（含釘選與等待／落後）、
  營業時間比對、候選口袋、移動卡片、上路模式（下一站）、雲端同步（推送與拉取都實測過）。
- **還沒實機驗過的**：拖曳排序（程式寫好了但沒真的用手指拖過）、
  撞車（conflict）的兩顆按鈕、類別新增與刪除、匯入 Google 網址（缺 key）。

### 設計
- 視覺世界是 impeccable 方向輪選出來的「手冊分頁板」（catalog `rw-manual-acetate-tab-board`，
  seed 20291719）。設計契約寫在 `index.html` body 開頭的 HTML 註解裡。
- 使用者陸續給的修正都已經套用：加圓角、拿掉活頁紙打孔點、分頁改成順時針直排的
  `Day 1` ＋日期、卡片壓低（69px）、字級縮一階、路線鍵不反白、**取消暗色主題**、
  「上路」改名成「下一站」、口袋分頁那個看起來像缺字的「口」換成張數。

---

## 已完成的三件事（色票、底色、字體）

1. **色票**：使用者親自挑定，見下方「已完成：色票」。**F5 `#E4E5E3` 已於 2026-08-29 移除**
   （對象牙白紙只有 1.17:1，DAY 色塊、分頁鐵、時間軸都幾乎看不見，補邊救不回來）。
   現在是八色，八天不重複。
2. **底色**：暖象牙白 `#FBF6EA` 已套用，當日色改當強調色。
3. **字體**：標題與站名走 `HelveticaNeue-CondensedBold` ＋ 中文 `Songti TC`；
   內文走 `PingFang TC`；數字與英文走 `Avenir Next` ＋ `tabular-nums`。
   全部字重上限 600——蘋方沒有 700，寫 700 會被 iOS 演算法假造粗體，
   那才是使用者說「字體太醜」的真正原因。

## 還沒做的收尾工作

- [x] 建 GitHub repo 與 GitHub Pages 部署（已完成：github.com/jeremyl861225/travel）
- [ ] 拖曳排序的實機驗證
- [ ] impeccable 的 finish review（`impeccable-finish-reviewer`）與 DESIGN.md（`impeccable-documenter`）
- [ ] 清掉測試資料：Supabase 上有一筆代號 `LKUD6TAH`、密碼 `travel2026` 的
      「京都五日」測試旅程（假資料）。**暫停期間刻意留著**，讓使用者可以在 iPhone 上
      開 https://jeremyl861225.github.io/travel/ 直接輸入這組來試用。定案後要刪。
- [ ] `sw.js` 的 `CACHE` 目前是 **`travel-v4`**，每次改版要記得跳號，
      並同步改 `index.html` 裡的 `BUILD` 字串（設定頁最下面會印出來）
- [ ] **維基百科照片模組：使用者已明確表示不做**（2026-08-29「不要自動接照片」）。
      作法與地雷留在最下面當紀錄，不要主動實作。

---

## 踩過的坑（別再踩一次）

1. **Supabase 免費方案每人 2 個 active 專案**，第三個建不起來，錯誤訊息會明講。
2. **`RETURNS TABLE` 的欄位名會跟資料表欄位撞**（42702）。凡是 `SET x = x + 1`
   都要寫成 `SET x = 資料表名.x + 1`。
3. **Google Maps 的 key 不能設 HTTP referrer 限制**——呼叫是從 Supabase 的伺服器
   發出去的，不是瀏覽器，設了會全部被擋。安全靠的是 key 從不進前端。
4. **Places API 的欄位分三級計費**：`rating`／`regularOpeningHours`／
   `internationalPhoneNumber`／`websiteUri` 會把整個請求推到 Enterprise SKU
   （每月只有 1,000 次免費）。個人用綽綽有餘，但別在迴圈裡打。
5. **`check.mjs` 一開始自己有 bug**：不認得正規表達式字面值，把 `/[&<>"']/g` 裡的
   引號當成字串開頭。已修（加了 `strip()`）。
6. **Browser pane 的 `computer` 點擊在面板被隱藏時會 timeout**，
   驗證互動時改用 `javascript_tool` 直接呼叫。
7. **`writing-mode:vertical-rl` 配 `flex-direction:column`** 才會讓子元素變成
   並排的兩欄（Day 1 一欄、日期一欄）；用 `row` 會變成同一欄接續排下去。

---

## 待執行：邊角與互動狀態（2026-08-29 使用者指定，尚未動手）

使用者傳了參考圖裡「元件狀態」那一區的截圖（BUTTON · PRIMARY／SECONDARY、
INPUT · TEXT，各有 NORMAL 與 ACTIVE 兩態），要求**區塊的邊角與互動照這張做**。
以下是從圖上讀到的規則。**hex 是目測，執行時要回原圖 `docs/refs/` 取樣，不要照抄。**

### 邊角
- 參考圖的圓角**比現在小一階**。控制元件（按鈕、輸入框）在約 60px 高的情況下，
  圓角看起來是 8px 上下，換算成半徑約為高度的 13%。
- 現在的值是 `--r:13px`（卡片）／`--r2:9px`（控制元件），**控制元件要收到 6–7px**；
  卡片可以維持略大，但兩者要有明確階層，不是同一個值。
- 按鈕與輸入框用**同一個**圓角值，這是參考圖很明顯的一致性。

### 按鈕 · 主要（primary）
- **NORMAL**：鉻黃實心填色（約 #EFC23F），外加一圈**細的深色邊框**（約 1px 近黑）。
  文字是深墨色的全大寫粗體，加字距，**置中**；箭頭 `→` **固定貼右緣**，不參與置中。
- **ACTIVE**：填色不變、顏色不變——**只有邊框變粗變深**（約 2–3px 實心近黑），
  下緣帶一點位移感。
- 重點：按下去的回饋是**邊框加重**，不是換色、不是變暗、不是加陰影。
  現在的 `.btn:active{transform:translate(1px,1px)}` 要換成這個作法。

### 按鈕 · 次要（secondary）
- **NORMAL**：紙白填色（比頁面底色再白一點），1px 灰邊框，深墨文字，`+` 貼右緣。
- **ACTIVE**：**整塊反相**——近黑底（約 #1A1A1A）、白字、白色圖示。
- 重點：次要鍵的按下狀態是**整塊翻黑**，跟主要鍵的「邊框加重」是兩套不同的回饋，
  刻意區分主次。

### 輸入框
- **NORMAL**：底色與頁面同色或再白一點，1px 淺灰邊框，佔位字是中灰，左對齊，內距寬鬆。
- **ACTIVE（focus）**：邊框換成**強調色（鉻黃）並加粗**，文字轉為深墨，游標可見。
  **沒有** outline、沒有 glow、沒有陰影——只有邊框顏色與粗細改變。
- 現在的 `.inp:focus{outline:2px solid var(--board);outline-offset:-1px}` 要改成邊框式。

### 共通
- 所有標籤是全大寫、加字距的粗體無襯線，置中。
- 圖示一律貼在右緣，與文字的置中互不干擾（文字對整個按鈕置中，圖示絕對定位在右）。
- 狀態之間**沒有過場動畫**，或最多維持現在的 90ms 兩格步進。

### 強調色＝當日色（2026-08-29 使用者已裁定）
參考圖用的是固定的鉻黃，但**這支 app 改成「當日色就是強調色」**：
主要按鈕的填色、輸入框 focus 的邊框、類別方磚、分頁鐵，全部跟著當天的顏色走。
換一天，整個介面的強調色就換一次。這在底色改成暖象牙白之後才成立——
底色是紙，當日色是唯一的彩色來源。

由此衍生、實作時一定會撞到的四件事：

1. **按鈕上的字色要每天重算。** 15 色裡有亮有暗，主要按鈕是實心填色配全大寫粗體字，
   字色必須用 `inkOn(當日色)` 動態選黑或白，不能寫死。
2. **中間亮度的顏色兩邊都不夠。** 相對亮度落在 0.18–0.22 的顏色，配黑字與配白字的
   對比都會低於 4.5:1。要在套用時檢查：若 `max(對黑對比, 對白對比) < 4.5`，
   **只針對按鈕**把填色往亮或暗推到足夠為止，分頁鐵與方磚維持原色不動
   （那兩處沒有字壓在上面，或字級夠大）。
3. **次要鍵的 ACTIVE 維持翻黑，不要跟著當日色。** 主要鍵是「當日色＋邊框加重」、
   次要鍵是「整塊翻黑」，兩種回饋刻意不同；次要鍵若也染上當日色，主次就分不出來了。
4. **勘誤條的朱紅是固定的，不能跟當日色撞。** 15 色裡若有顏色與勘誤朱紅感知距離太近，
   那一天的紅色警告會失去「這是異常」的意義。分群完要跑一次檢查，
   撞到的色移到群尾（15 天內排不到的位置）。

---

## 已完成：色票（2026-08-29 使用者親自挑定）

分群成四組每組 15 色的版本**被否決**——使用者看過後說「色票太相近了」，
要求回到最初的 5 色一組讓他自己挑。分群工具 `tools/group-palettes.mjs` 留著，
原始 60 色在 `docs/refs/colormind-raw.json`。

**最後選定的九色**（F 組全五色 ＋ G1/G2/G3 ＋ C1，順序照他給的，沒有重排）：

| | hex | 名稱 | 對紙對比 | 當填色時最佳字對比 |
|---|---|---|---|---|
| F1 | `#9D914A` | 橄欖金 | 2.96 | 5.92 |
| F2 | `#342F32` | 近黑暖灰 | 12.18 | 13.14 |
| F3 | `#597A66` | 苔綠 | 4.43 | 4.77 |
| F4 | `#909D7E` | 鼠尾草 | 2.66 | 6.57 |
| F5 | `#E4E5E3` | 近白 | **1.17** | 14.94 |
| G1 | `#6A3D4F` | 李 | 8.14 | 8.77 |
| G2 | `#806386` | 藕紫 | 4.81 | 5.19 |
| G3 | `#907DCE` | 長春花 | 3.24 | 5.40 |
| C1 | `#304750` | 板岩藍 | 9.09 | 9.80 |

九色＝九天不重複，第十天起循環。

**F5 是這組唯一的弱點**：對象牙白紙只有 1.17:1，當日色輪到它的那天，
DAY 色塊、類別方磚、分頁鐵、每日長條圖幾乎都是空白的。
處理方式是**不改他挑的顏色**，改在 `applyBoard()` 判斷「對紙對比 < 1.6」時
發布 `--accent-edge`，給方磚與色塊補一道髮絲邊，形狀才讀得出來。
真的要救，得換掉 F5 或把它排到第 10 位以後（多數行程排不到）。

---

## 從 tohoku-2026 學到的（2026-08-29）

使用者要求去看 tohoku-2026 有沒有可用的點子或模組。那份 `CLAUDE.md` 記了很多
用實測換來的教訓，以下是與 Travel 相關的。

### 已經套用進 Travel 的兩個

1. **Google Maps 的地名連結會安靜地指到錯的地方。**
   Google 查不到地名時不報錯，會退回「搜尋使用者所在地附近的同類地點」。
   tohoku 實測到 `山湖台展望所` 指到台北碧山里（差 2,239 km）、
   `大峠トンネル` 指到 463 km 外的同名隧道。
   → `gmPlace()` 已改成 **place_id ＞ 座標 ＞ 名稱** 的順序。手動建立、沒抓過
   Google 資料的卡片本來會用名稱查，正是會中招的那一種。
2. **同一個 origin 下每個 repo 共用 `caches`，SW 的 `fetch` 只能攔自己子路徑。**
   → `sw.js` 已加上 pathname 前綴檢查。`activate` 只刪自己前綴的快取本來就有做。

### 建議採用但還沒做：維基百科照片模組

tohoku 的做法是**完全不打包照片**，改由瀏覽器端向日文維基要圖，失敗就退回自繪
SVG 圖示。這對 Travel 很有價值——使用者選了「圖片只存網址」，一部分原因是
Google 的景點照片要另外計費；維基是免費、免 key、CORS 開放，而且可以進 SW 快取離線看。

實作方式（照抄 tohoku 的順序）：
1. 先試 Action API 加 `origin=*`，用 `pithumbsize=900` 指定尺寸
2. 失敗改用 REST API `ja.wikipedia.org/api/rest_v1/page/summary/<title>`
3. 兩條都失敗 → 退回類別方磚，不破圖

**四個會讓照片全部不出現、而且完全不報錯的地雷（tohoku 已實測修過，不要重犯）：**
1. **不可自己改寫縮圖網址的寬度。** Wikimedia 只供應 API 實際產生的那一個尺寸，
   把 `/320px-` 換成 `/900px-` 一律回 400（實測 330 可、640/800/900/1024 全部 400）。
   要大圖就用 Action API 的 `pithumbsize` 去要。
2. **`<img>` 必須設 `crossOrigin='anonymous'`，而且要在指定 `src` 之前設。**
   不設就是 no-cors，拿到 opaque 回應，`cache.put()` 存不進去；那個被拒絕的 put
   還會讓該次請求卡住不返回。
3. **在記憶體裡建、等 `onload` 才插進 DOM 的 `<img>` 絕對不可以加 `loading="lazy"`。**
   瀏覽器會因為「元素不在文件裡」而永遠不開始載入，onload 不觸發，圖永遠插不進去。
4. **照片必須等 Service Worker 接管後才載入。** SW 註冊掛在 `window load`，
   第一次造訪時照片會在 SW 接管前就抓完、整批繞過離線快取。tohoku 的解法是注入
   `window.DEFER_PHOTOS=true`，`render()` 照跑但 `loadPhotos()` 等
   `serviceWorker.controller` 出現（或 4 秒逾時）才呼叫。

另外：**照片快取要用不帶版本號的 cache 名稱**（tohoku 用 `tohoku-guide-media`），
改版時才不會被 `activate` 清掉。

**要先問使用者**：這會讓每張卡片自動長出一張照片，跟他原本「圖片只存網址」的
決定不同（不衝突，是多一個來源）。日文維基對日本景點命中率高，其他國家未必。

### 還可以參考但目前用不到的
- **每天第一段路會消失的 bug**：tohoku 的 `render()` 在日界把 `prev` 清成 `null`，
  於是每天早上從飯店出發的那一段整個不見。Travel 的資料模型不同（`leg` 掛在起點卡、
  住宿卡靠「複製到隔天開頭」補），但同一類的破口要留意。
- **地圖標記不要放太近**：4 km 在 396 px 寬的地圖上只差 5 px，標籤會疊住。
  Travel 目前沒有自繪地圖，之後若做「當日全程圖」會用到。
- **一律不引入外部 CDN**（山區沒網路）。Travel 已符合。

---

## 2026-08-29 後記：手機看到的是舊版

使用者回報「字體還是舊的」「頁尾空白還在」，但同一份程式在桌機瀏覽器上
中文明明已經是宋體。原因是 service worker 的 stale-while-revalidate：
**改版後第二次開才會換成新版**，而使用者每次都是開一次看一眼就關掉。

處理方式（已實作）：
1. `sw.js` 的 `install` 本來就有 `skipWaiting()`、`activate` 有 `clients.claim()`，
   所以新 sw 會立刻接管 → 前端監聽 `controllerchange`，一接管就 `location.reload()` 一次。
   旗標防重入；並且**第一次造訪不重載**（那次本來就沒有 controller，
   `clients.claim()` 也會觸發同一個事件，重載是白費的）。
2. 設定頁最下面印出 `BUILD` ／ 獨立或瀏覽器 ／ `--vh` ／ `innerHeight` ／
   `visualViewport.height` ／ `--sab`。以後再遇到「改了沒生效」或「頁底對不齊」，
   叫使用者唸這一行就有答案，不用再猜。

### 頁底空白：真正的成因與現在的作法
`--sab`（home indicator）本來是**整條加在操作列的 padding-bottom**，
結果按鈕黏在操作列上緣、下面留一條 34pt 的空白，上下不對稱，看起來就是破了個洞。
改成：`--sab` 只算進整條的高度，`padding-bottom:0`，內容垂直置中 →
實測 375×812 模擬 `--sab:34px` 時，按鈕上方 27.5px、下方 26.5px，對稱。

另外 `fitViewport()` 在**獨立模式改用 `max(innerHeight, visualViewport.height)`**：
獨立模式沒有瀏覽器工具列，`innerHeight` 就是整個螢幕；而 `visualViewport`
在啟動動畫還沒結束時會回報偏小的值，量到就定住，頁底永遠少一截。
瀏覽器模式維持用 `visualViewport`（`innerHeight` 含被工具列蓋住的那一段）。
啟動後 0/120/400/1000ms 各重量一次，並監聽 `pageshow`。
