# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

單檔純靜態：一支 `index.html`（內含 CSS 與 JS）＋ `sw.js` ＋ `manifest.webmanifest`，無建置步驟，push 到 GitHub Pages 即為正式版。使用者親自選定，理由是與他既有的 PWA（todo-app、patient-list、tohoku-2026）一致、事後想自己改一行字不必重跑 build。

代價已知：語法錯誤不會在建置期被攔下，只會在瀏覽器變成 `Unexpected end of input`（他在 todo-app 踩過）。因此本專案附一支 `check.mjs`（Node，僅開發期使用，不進入執行路徑），每次交付前必須跑過：解析 `index.html` 內所有 `<script>` 的語法、檢查括號平衡、掃出未定義的函式引用。

後端為 Supabase（新專案）：Postgres 存密文、Edge Function 當 Google Maps 代理。

## Users

主要使用者是這位使用者本人與**同行的旅伴**（家人或朋友，2–6 人），用 iPhone 操作。他是外科醫師，行程規劃時間破碎——通勤、值班空檔、睡前各排一點，因此每次開啟只有幾分鐘，必須能立刻接續上次的地方。

旅伴不一定裝得起任何帳號系統：他們拿到的只有一組「旅程代碼＋密碼」，輸入就能一起編輯。

## Product Purpose

把一趟旅行從「一堆分頁、截圖與 LINE 對話」收斂成一份會自己算時間的每日行程表。

使用者做的事只有兩件：把景點放進某一天、排好順序。**其餘由程式負責**——到達時間、路程時間、營業時間對不對得上、當天能不能走完。

成功的定義：出發當天早上，打開就知道今天幾點在哪裡、下一站怎麼去；而且行程是全團同一份，不是各自一份。

## Positioning

一般行事曆把行程當「事件」，時間要自己填；一般旅遊筆記把行程當「清單」，沒有時間概念。

這支 app 把行程當成**一條會自己推算的鏈**：每張卡片只需要「停留多久」，到達時間由「前一站的離開時間 ＋ 兩站之間的實際路程」推出來。因此拖動一張卡片的順序，後面所有時間會即時重算——這是在別處要靠手算的部分。

再加上兩件別人不做的事：卡片可以「釘選」固定時間（預約制的餐廳、寺院），推算會繞著釘選點前後對齊並在來不及時標紅；以及不需要註冊帳號的共編（代碼＋密碼，內容端對端加密）。

## Operating Context

- **行前規劃**與**旅途中查看**並重，兩種模式都要做好：規劃時編輯密集、資訊密度高；上路時要大字、單手、一眼看完下一站、且**沒有網路也要能用**（國外漫遊不穩是常態）。
- 景點來源幾乎都是 iPhone 上 Google 地圖 App 的「分享」→ 產生 `maps.app.goo.gl` 短網址，直接貼進來。
- 多人同時編輯是常態（例如兩人各排不同天），不能互相覆蓋。
- 旅程可同時存在多趟，走完的行程保留成歷史，可複製成下一趟的底稿。

## Capabilities and Constraints

**行程卡片**：名稱、類別、地圖連結、到達時間、停留時間；展開後有介紹、網址連結、預定事項（可勾選的完成框）、圖片（只存網址，不上傳檔案）。

**類別**：預設「自然／古蹟／購物／互動／飲食／住宿」，可自行新增。住宿是保留類別，帶有「複製到隔天開頭」的動作（依使用者決定，**不自動連動**）。

**時間**：每天設一個出發時間，其餘卡片依「停留＋路程」往下推；任一張卡可釘選固定時間，前後自動遵守，推不到就標紅。

**路程**：卡片與卡片之間顯示路程時間，可點開 Google Maps 以上一張為起點、下一張為終點導航。

**排序**：卡片可拖曳調整順序，也可移到別天或移進候選口袋清單。

**Google 資料**：貼上 Google 地圖網址自動帶入名稱、地址、電話、座標、營業時間與公休、評分與評論數。**不抓 Google 官方照片**（另計費且網址有時效）。

**加值**：候選口袋清單（想去但還沒排進哪一天）、一鍵開當日全程多點導航、每日天氣（Open-Meteo，免 key，僅未來 16 天內有值）。

**明確不做**：花費／預算欄位、帳號註冊、上傳圖片檔。

**技術約束**：
- API key 絕不進前端。短網址展開、Places、Routes 一律走 Supabase Edge Function，key 存在伺服器端 secret。
- 內容在瀏覽器內以 AES-GCM 加密後才上傳，金鑰由旅程密碼經 PBKDF2 導出；伺服器看不到任何行程內容，**密碼遺失即無法復原**。
- 本機優先（local-first）：離線可讀可編輯，回到連線再同步；每張卡片獨立同步，兩人改不同卡片不會互相蓋掉。
- Google Maps Platform 需綁信用卡；使用者自行申請並設每日上限，程式端必須對「額度用盡／key 未設定」有明確的降級行為（改為手動填寫，不可整頁失效）。

## Brand Commitments

名稱為 **Travel**。介面語言為繁體中文（台灣用語）。部署於 `jeremyl861225.github.io/travel`，需可從 iPhone 加入主畫面並離線啟動。

## Evidence on Hand

無既有品牌資產、無設計稿、無使用者研究。同一位使用者的既有 PWA（`tohoku-2026`、`todo-app`、`patient-list`）僅作為工程慣例參考，**不構成本專案的視覺權威**。

`tohoku-2026` 內有一份已完成的南東北六日自駕行程，可作為真實資料樣本。

## Product Principles

1. **時間是程式的責任，不是使用者的。** 使用者只給「停留多久」與順序；任何要人心算的欄位都是設計失敗。
2. **一趟行程只有一份。** 共編是預設狀態，不是附加功能；任何介面都要假設另一個人正在改別的地方。
3. **沒有網路是常態，不是例外。** 離線時能做的事要跟連線時一樣多，只有「抓 Google 資料」可以等。
4. **上路時的一眼，勝過規劃時的一切。** 資訊密度可以在規劃模式很高，但在街上必須三秒內讀完下一站。
5. **抓不到資料要能手填。** 每一個自動欄位都必須有等價的手動入口，額度用盡不會讓行程變成廢紙。

## Accessibility & Inclusion

戶外強光下單手操作為主要情境：主要文字對比需達 WCAG AA 以上，觸控目標不小於 44×44pt，關鍵操作留在拇指可及範圍。類別**不可只用顏色區分**（同時用圖示與文字），旅伴中可能有色覺辨識差異者。
